# ===============================================
# .env
# SEMPRE carregado — independente do modo usado
# NÃO é sobrescrito — ele fornece valores padrão
# Pode ser sobrescrito por:
#    - .env.local
#    - .env.[mode]
#    - .env.[mode].local
# ===============================================

# Nome da aplicação
VITE_APP_NAME=OrbitalDev

# URL padrão da API (caso não seja sobrescrita)
VITE_API_URL=http://localhost:7754

# ⚠️ Todas as variáveis precisam começar com VITE_ para serem acessíveis no código React
